{
  "name": "create-tables-for-existing-instance",
  "description": "Create OTS instance and multiple tables",
  "provider": "alibabacloudstack",
  "usecase": "This example demonstrates how to create an OTS instance with multiple tables in batch mode.",
  "inputs": [
    {
      "name": "instance_name",
      "description": "The name of the OTS instance to create",
      "required": false,
      "default": "tfots2"
    },
    {
      "name": "table_schemas",
      "description": "Map of table names to their primary key definitions",
      "required": false
    }
  ],
  "outputs": [
    {
      "name": "existing_instance_name",
      "description": "The name of the created OTS instance"
    }
  ]
}
