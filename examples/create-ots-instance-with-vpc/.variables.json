{
  "instance_name": {
    "description": "The name of the OTS instance to create.",
    "type": "string",
    "optional": true,
    "i18n": {
      "zh-Hans": "要创建的OTS实例名称。",
      "en-US": "The name of the OTS instance to create."
    }
  },
  "table_schemas": {
    "description": "Map of table names to their primary key definitions (key=name, value=type)",
    "type": "map(map(string))",
    "optional": true,
    "i18n": {
      "zh-Hans": "表名到主键定义的映射（键=名称，值=类型）",
      "en-US": "Map of table names to their primary key definitions (key=name, value=type)"
    },
    "default": { 
    }
  }
}